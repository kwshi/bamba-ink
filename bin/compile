#!/bin/sh -eu

BUILD_DIR=$1
CACHE_DIR=$2
PROJECT_DIR="$BUILD_DIR/server"
TMP_DIR=$(mktemp -d "$CACHE_DIR/tmp-XXXXXX")

mv -T "$PROJECT_PATH" "$TMP_DIR"
rm -rf "$BUILD_DIR"
mv -T "$TMP_DIR" "$BUILD_DIR"
